<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Restart the Game{% endblock %}</title>
        <meta name="author" content="Olivier Haag">
        <link rel="icon" href="{{ asset('favicon.ico') }}" />


        {% block stylesheet %}
            {% stylesheets
                'bundles/rtgsite/less/bootstrap.less'
                'bundles/rtgsite/less/main.less'
                filter='cssrewrite'
                output='css/main.css'
            %}
            <link rel="stylesheet" href="{{ asset_url }}" />{% endstylesheets %}
        {% endblock %}
    </head>
    {% trans_default_domain 'RTGSiteBundle' %}
    <body>
        <div class="scanlines"></div>
        {{ include('RTGSiteBundle:Page:menuUser.html.twig') }}
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <div class="header-logo">
                        <a href="{{ path('rtg_site_page_index') }}" title="Restart the Game">
                            <img class="center-block img-responsive" src="{{ asset('bundles/rtgsite/img/logo.png') }}" alt="Restart the Game"/>
                        </a>
                    </div>
                </div>
                <div class="col-md-9">
                    <div class="title row">
                        <div class="center-block">
                            <a href="{{ path('rtg_site_page_index') }}" title="Restart the Game">Restart the Game</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div id="main-menu" class="container bloc">
            {{ render(controller('RTGSiteBundle:Page:menu')) }}
        </div>
            
        {% block body %}{% endblock %}
        
        <div id="footer">
            <div class="container bloc">
                <div id="copyright">
                    {% block footer %}
                    © 2013 Restart the Game - <a href="{{ path('rtg_site_page_legalnotices') }}">{{ 'legal_notices'|trans({}, 'menu') }}</a>
                    {% endblock %}
                </div>
            </div>
        </div>

        {% block javascript %}
            {% javascripts
                'bundles/rtgsite/js/jquery-1.9.1.min.js'
                'bundles/rtgsite/dist/js/bootstrap.min.js'
                'bundles/rtgsite/js/main.js' %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        <script>
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
            ga('create', 'UA-46631318-1', 'auto');
            ga('send', 'pageview');
        </script>
            {% endjavascripts %}
        {% endblock %}
    </body>
</html>