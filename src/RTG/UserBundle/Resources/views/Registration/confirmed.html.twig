{% extends "RTGUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block title %}Confirmation d'email{% endblock %}

{% block content %}
    <h1>Confirmation d'email</h1>
    <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
    {% if app.session is not empty %}
        {% set targetUrl = app.session.get('_security.' ~ app.security.token.providerKey ~ '.target_path') %}
        {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
        <p>Aidez nous en complétant votre profil !</p>
        <a href="{{ path('fos_user_profile_edit') }}" title="Profil" class="btn btn-primary">Compléter mon profil</a>
    {% endif %}
{% endblock %}
